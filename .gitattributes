# Normalize line endings: LF in the repo, checked out as-is. Prevents the
# CRLF/LF churn warnings on Windows and keeps diffs clean across platforms.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
