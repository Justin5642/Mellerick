# Mobile Build — Decision Log

Non-crucial questions/decisions indexed for the cleanup phase (MP11). Crucial items
are flagged to Avi in real time per the plan's crucial-flag protocol.

## Crucial blockers (flagged to Avi, block specific phases)

| # | Item | Blocks |
|---|---|---|
| B1 | `SUPABASE_DB_URL` (Postgres connection string) — needed to set up the PowerSync publication + connector | MP3 |
| B2 | PowerSync Cloud account (AU region) + monthly-cost approval | MP3 |
| B3 | Apple Developer ($99/yr) + Google Play Console ($25 once) accounts | MP10 store submission |
| B4 | Expo push credentials (APNs key / FCM) | MP9 push |

## Decisions made (with rationale)

| # | Decision | Why |
|---|---|---|
| D73 | **E2E flow specs authored (MP11 e2e — Maestro).** The plan's Definition-of-Done names on-device e2e (login→my-jobs→clock-in→complete; dashboard→approvals→approve; technician-sees-no-dollars; offline clock-in). Authored a `.maestro/` suite covering exactly those: `01-technician-clock-in`, `02-office-approve-job`, `03-technician-money-gating` (asserts the `.*\$[0-9].*` regex is never visible on any technician surface — the end-to-end complement to the MoneyText/route-gating unit tests), `04-offline-clock-in` (airplane-mode clock-in survives reconnect via the durable outbox; Android-only per Maestro's setAirplaneMode), a reusable `subflows/login.yaml`, `config.yaml`, and a README (prereqs, env-var credentials, run + CI commands). Added stable `testID`s to the driven elements (`login-email/password/submit`, `job-list-row` [already present], `clock-in`, `clock-out`, `approve-job`). All flows validated as well-formed YAML; 200 unit tests still green, tsc + bundle clean. **NOT executed** — there is no simulator/emulator or seeded backend here, so they are an executable spec ready for the on-device-QA gate (a device/emulator + per-role test accounts). | Fulfils the e2e deliverable in-repo as far as possible without hardware: the critical journeys + the money-gating security invariant are now codified as runnable flows, so the on-device QA gate becomes "run these" rather than "design these" |
| D72 | **GATE 1 backend Bearer enablement — DRAFTED + tested (PROPOSED for Jason, like RLS 0035).** The biggest external blocker was that the web Invoice/Quote **Send-email / PDF** routes were cookie-only, so a mobile Bearer call 401'd. Root cause pinpointed: `lib/api/guards.ts` already resolves *identity* from a Bearer token, but the four routes then built their DB client with the cookie-session `createClient()`, so a mobile caller was authorized yet its RLS-scoped `.from(...)` ran with no session. **Fix (additive, low-risk):** new `lib/api/caller-client.ts` → `callerClient(request)` returns a Supabase client scoped to the caller — a Bearer token yields an anon-key client whose `Authorization` header is the caller's JWT (RLS runs as them); **no token → the existing cookie client, byte-for-byte unchanged**, so web behaviour cannot regress (the change only *adds* the previously-404'ing mobile path). Refactored all four routes (`app/api/invoices/[id]/{send,pdf}`, `app/api/quotes/[id]/{send,pdf}`) to `requireOfficeOrAdmin(request)` + `callerClient(request)`. **12 web unit tests** (caller-client Bearer-vs-cookie contract + a 401/403 authorization matrix for all four routes proving the guard short-circuits before any DB/PDF/email). Web `tsc` clean, 80 web unit tests green. The mobile `ApiBridge` **already** sends `Authorization: Bearer <access_token>`, so this is directly consumable. **NOT deployed** — it's on `mobile/full-parity` for **Jason to review + merge + deploy** (I can't run the live web app / real sessions here); the same `callerClient` pattern is the template for the Xero push routes when he tackles those. Then the mobile Send/PDF buttons are a fast follow. | Turns GATE 1 from "needs a backend refactor" into a reviewed, tested one-file-helper + four one-line swaps; scoped so the web path is provably unchanged, which is why it was safe to draft against shipped routes rather than only describe it |
| D1 | Scope = full parity, all 15 areas, all roles (Avi-confirmed), native-redesigned (capability parity, not desktop pixel-clones) | Avi's explicit goal; both consensus models require native redesign of dense/desktop patterns |
| D2 | Offline = PowerSync (Avi-confirmed) despite the offline agent + gemini recommending a lighter outbox; PowerSync sync rules treated as security-reviewed code with role-impersonation tests | Avi's choice; guardrails mandatory because PowerSync bypasses RLS/views on the sync path |
| D3 | Dollar-leak RLS tightening (inventory/job_expenses/equipment/PO/cost_center_templates → office/admin) is MANDATORY and lands first (MP1) | Both models: UI hiding can't stop DB disclosure; PowerSync would replicate $ to tech phones otherwise |
| D4 | UI = NativeWind v4 (port web Tailwind tokens + badge-colors 1:1) + a new dark theme the web never built | Web-token reuse, team Tailwind familiarity, Expo-endorsed; premium via design system not framework |
| D5 | Icons switch Ionicons → lucide-react-native | Exact parity with the web icon set |
| D6 | Background auto-clock: foreground-only for early phases; background build in MP9 | Needs custom dev client; not required for first shippable |
| D7 | Branch mobile/full-parity off main; MP1 touches shared supabase/ → PR, coordinate with Jason (active on repo) | Avoid colliding with Jason's concurrent work |
| D8 | Build the offline write-outbox layer NOW (durable SQLite outbox + processor + repositories), behind a swappable data layer, and connect PowerSync later behind the same interfaces ("do both") | Avi asked for both; the outbox needs no browser/password so it unblocked progress while the PowerSync DB-password step (B1) is pending. Repositories are the only place table names live → PowerSync slots in without touching screens |
| D9 | `WriteOperation` carries a distinct operation-id (outbox PK) AND rowId (target row PK); side-effect coalescing adopts the latest trigger's `dependsOn` | Correctness: without the split, an offline clock-in (insert) + clock-out (update) to the same row collided on the store PK and lost the insert. Both fixed with regression tests |
| D10 | Offline read model (interim): reads refresh from the server only when online; offline, local state (incl. optimistic writes) is authoritative. **Consequence: offline COLD-START shows an empty time log until the first online load** — within-session offline writes are visible via optimistic rows | Full offline reads = the persisted read-cache (the other half of the outbox design) or PowerSync buckets, deferred. Keeps online UX identical to today and makes offline writes durable + visible without a half-built cache that could show stale/partial data |
| D11 | Parity: deleting a time entry and assigning a cost-center do NOT resync billing (matches the web app exactly) | Web's deleteEditing/assignCostCenter don't call syncBilling. Deleting an entry that contributed hours arguably should shrink the labour line item — flagged as Q6, a possible pre-existing web bug, not "fixed" unilaterally to preserve parity |
| D12 | Photo delete removes the Storage object BEST-EFFORT (never throws). Photos' outbox delete carries storage_path so cleanup works the instant a policy exists | **The `job-photos` Storage bucket has no DELETE policy** (only INSERT + SELECT). So `storage.remove()` is silently RLS-denied *today* in BOTH web and mobile — deleted photos' objects already orphan in the bucket; only the DB row is removed. If `removeObject` threw, every offline photo delete would wedge on retry. Fix = add a storage.objects DELETE policy for job-photos (touches shared `supabase/` → coordinate with Jason, see Q8) — then object cleanup starts working with zero mobile code change |
| D13 | Photo Storage object key derived from the client rowId (`${jobId}/${rowId}.jpg`), not `Date.now()_rand` as the old screen did | A stable key tied to the row makes offline replay idempotent — a retried upload hits the same object (upsert) and the same row re-inserts, so a flaky network can't scatter duplicate objects. Behaviourally invisible (storage_path is just an opaque key) |
| D14 | Queued attachments are copied into a durable app-documents dir (`outbox-attachments/`) before queueing, and deleted after the write syncs (gateway.cleanupAttachment) | Picker/camera files live in volatile cache the OS can purge before sync; copying guarantees the photo survives restarts. Cleanup-after-sync stops the dir growing without bound (kept on failure for retry) |
| D15 | Signature and Voice-report are SEPARATE compound verticals, NOT built in this Photos/Notes pass | Recon captured them fully: signature = job_photos insert (photo_type='signature', caption) + a `jobs` UPDATE (status=completed, ready_to_invoice, completion_notes, actual_end) + a `sync-calendar` side-effect; voice = `job-audio` upload + a `transcribe-voice-report` side-effect (effectively online-only). They cross into the `job` aggregate + calendar/transcribe side-effects and are the natural next vertical (job-completion). See Q9 |
| D17 | Signature sign-off = a compound offline write: (1) signature image insert into job_photos, (2) jobs UPDATE (completed/ready_to_invoice/completion_notes/actual_end), (3) sync-calendar side-effect depending on the jobs update. Photo insert and jobs update are INDEPENDENT (not gated on each other) — matching web | Web does the two writes sequentially without gating; the job completes even if the image upload has trouble, and the image retries durably. Trade-off: in the rare case the image upload dead-letters, the job is completed without its signature image (surfaced via the sync badge). sync-calendar depends on the jobs update so the calendar reflects the completed status |
| D18 | Voice report is now DEFERRED / durable (was online-only + synchronous inline transcript). The recording queues through the outbox and transcribes on reconnect; the transcript appears on the next job-detail load, not inline. Coalesced per job (latest recording wins) | Offline durability is the whole point of this initiative — a recording made with no signal must not be lost. The transcribe route is effectively online-only and last-write-wins, so a coalesced side-effect that retries is safe (a lost response just re-transcribes the same audio). UX change: the recorder now confirms "Voice report saved — transcribes when online" instead of showing the transcript immediately |
| D19 | Added an `upload` WriteOp (upload-only, writes NO metadata row) for the voice audio | The transcribe route writes the jobs.voice_report_* columns server-side from the uploaded object, so the client only needs a durable, ordered upload that the transcribe side-effect depends on — not a client-written row. Also: the sync-calendar side-effect now sends a Bearer token (via apiBridge) whereas the old signature screen posted with no auth header (Q1) — arguably more correct; confirm the route accepts it |
| D20 | Adversarial review of the Signature/Voice vertical found 5 defects + a critic-found engine gap — all fixed with regression tests | **(high)** I had wrongly made job-completion INDEPENDENT of the signature upload ("matching web" — but web returns early on upload failure, so completion IS gated on it). A job could become `ready_to_invoice` with no signature. Fixed: the jobs-completion write now `dependsOn` the signature-photo op (chain photo → jobs → calendar). **(med)** a re-record could coalesce onto an *inflight* transcribe and lose the latest transcript → coalescing now only targets pending/failed ops, never inflight/dead. **(critic, high)** an op whose dependency terminally died hung `pending` forever, invisible to the badges → the processor now cascades `dead` to dependents. **(low)** offline re-record orphaned the prior audio object → stable per-job audio key (`${jobId}/voice-report.m4a`) so it upserts. **(med, interim)** the signature screen shows "Signed off" optimistically even if the write later dead-letters — consistent with the D10 optimistic-UI model; **surfacing per-job/global sync failures needs the sync badge placed (Q10) — now more important since dead-letters are otherwise invisible in the UI** |
| D16 | Adversarial review (5-dimension workflow) of the Photos/Notes diff found 5 real defects — all fixed with regression tests | **(critical)** offline add-then-delete could resurrect a deleted photo (delete outran a backed-off insert) → a delete now `dependsOn` any not-yet-synced insert for the same row. **(high)** an op stranded `inflight` by a crash was never retried → the processor reclaims `inflight`→`pending` on each drain. **(medium ×3)** optimistic rows dropped/blanked on a reload triggered by a was-online guess → screens now reconcile on a real `onSettled` (post-drain) signal and MERGE (keep still-pending rows) instead of replacing. **(critic)** poison writes retried forever → terminal `dead` state after 8 attempts (surfaced via `useSyncStatus`); restored `try/finally` on the gallery/notes handlers so a staging error can't wedge the spinner |

## Office/admin shell (MP4 + first vertical)

| # | Decision | Why |
|---|---|---|
| D21 | Role-aware navigation via expo-router `Stack.Protected`: a separate `(office)` 5-tab group (Dashboard · Jobs · Schedule · Approvals · More) gated against the technician `(tabs)` group by role. Forbidden routes are NOT registered for the wrong role (RLS backstops). `(office)` has NO index.tsx (two files → `/` throws); office lands on `/dashboard`, techs on `/`. A fail-closed "No role assigned" screen covers a signed-in user whose profile has no recognizable role | The v6 `Protected` API is the intended mechanism for "forbidden routes never registered" (verified against the installed build). A second independent Tabs navigator keeps the technician tabs byte-for-byte unchanged. Admin-only areas (Staff, Settings) sit under a nested `guard={isAdmin}` + are shown in More only to admins |
| D22 | Scope of this office pass: **Dashboard fully built** (5 count KPIs — Active/Total Jobs, Customers, Overdue Invoices, Backflow Due — greeting, Today's + Recent job lists; **zero dollars**, matching web exactly). **Jobs / Schedule / Approvals** are functional read screens (real data, tap → job detail). **Approvals** lists `ready_to_invoice` jobs — exactly what the mobile sign-off flow sets, so a field sign-off surfaces to the office end-to-end. The 9 remaining areas (Customers, Quotes, Invoices, Pricing, Inventory, Fleet, Reports, Staff, Settings) are **routed + role-gated `ComingSoon` placeholders** to build out in MP6-MP8 | "Start on office/admin" = the nav foundation + the landing (Dashboard) + the operational read screens. The write-heavy financial/admin actions (approve→invoice→Xero, quotes/invoice builders, pricing/inventory CRUD, staff, settings) are the larger next blocks |
| D23 | Office screens read online-only (direct Supabase), no offline cache yet | Office users are typically at a desk online; offline reads = the deferred persisted-cache (D10/Q7) |
| D24 | Adversarial review of the office shell found 7 defects + a critic-found parity bug — the real ones fixed with the rest documented | **(critic, parity)** Approvals queried `ready_to_invoice=true` — that's the web *Invoices* queue, not *Approvals*. Fixed to match web exactly: `status=completed AND admin_status=pending` + the pending-variations queue. **(login flash)** every interactive login briefly showed the fail-closed "No role assigned" screen during the profile fetch (`onAuthStateChange(SIGNED_IN)` never re-raised `loading`) → `loadProfile` now sets `loading=true` first. **(timezone)** Dashboard "today" + Schedule day-grouping used device-local TZ → ported the web's business-TZ helpers (`lib/date.ts`, Australia/Melbourne) so they match web on any device. **(truncation)** office Jobs was hard-capped at 200 (older jobs invisible, search missed them) → server-side search (title/job#) + paginated infinite scroll. Backflow-shared-routes (#2) is intentional, not a bypass (see below); office-backflow (#5) + admin-field-access (#6) documented as gaps below |

## Financial areas (MP7 — first pass: read layer)

| # | Decision | Why |
|---|---|---|
| D25 | Built the financial READ layer: Invoices (list + detail), Quotes (list + detail), Pricing (list). All office/admin-only (RLS `is_office_or_admin` on invoices/invoice_items/quotes/quote_items/pricing_items — migration 0027; technicians get zero rows). Every dollar renders through `MoneyText` (role-gated, defense-in-depth even though techs can't reach these screens) | The read layer is the high-value, low-risk first slice — it gives office/admin financial visibility on mobile and establishes the money-display + status patterns. The list rows share a new `FinanceListRow`; details show customer + line items + Subtotal/GST(10%)/Total (GST hard-coded 10% exactly as web; `invoice_items.total`/`quote_items.total` are DB-generated, read-only) |
| D26 | **BLOCKER (backend):** Send-email, PDF, and Xero-push are NOT available on mobile. All the web routes (`/api/invoices/[id]/send`, `/pdf`, `/api/xero/push-invoice`, quotes equivalents) authenticate via the **cookie session only** (`lib/supabase/server.createClient` + `getUser`) — they never read the `Authorization` header and don't use the Bearer-aware `lib/api/guards.getCallerId`. A mobile Bearer call → 401. The detail screens show a "managed on the web dashboard" note instead of dead buttons | To make these mobile-callable, the web routes must be refactored to `getCallerId(request)` (guards.ts already supports Bearer). That's a web-side change to coordinate with Jason. Xero-push is additionally admin-only |
| D28 | Adversarial review of the financial read layer: **no money-leak** (MoneyText used on every dollar); 2 minor defects + a critic-found parity gap, all fixed | **(gap, most important)** the web invoices page shows a "Ready to Invoice" queue (jobs with `ready_to_invoice=true` + approved/auto_approved `job_variations` with `invoice_id IS NULL`) — unbilled work that surfaces nowhere else. Mobile omitted it → unbilled revenue "silently never surfaced." Fixed: the mobile invoices list now shows that queue as a header section (tap → job). **(parity)** quote numbers were `QUO-0007`; web uses a bare `#7` (only invoices get INV-padding) → matched. **(pagination)** invoices/quotes/jobs lists ordered by non-unique `created_at` with no tiebreaker (rows could dup/skip at page boundaries) → added an `id` tiebreaker. Minor divergence noted: mobile Pricing shows `is_active=true` only (web shows all) — a deliberate "usable items" reference view |
| D30 | Finance-write self-review (the Workflow multi-agent review was rate-limited — Claude weekly cap; done as a main-loop self-review instead). Findings: **(fixed)** Pricing CRUD had no optimistic update → offline saves vanished from view until sync; added optimistic add/edit/remove. **(documented residual)** Invoice/quote EDIT of a live doc: if an item insert is mid-retry, the itemized breakdown is transiently incomplete — BUT `invoice.total` is client-computed + written in the parent UPDATE (authoritative + correct), and the outbox self-heals the items on retry; a permanent dead-letter is surfaced by the sync badge. True atomicity still needs the flagged Postgres RPC (Jason). **(verified)** columns/GST(10%)/generated-total-avoidance match the schema + web | Keep the phase gate even when the subagent reviewers are unavailable — self-review + document the residual rather than skip |
| D31 | Customers area built (list + detail + sites) with full CRUD via the outbox + the read-repository layer. Self-review (main-loop; multi-agent review still rate-limited) found + fixed: creating a customer offline then navigating to its detail hit "not found" (the detail reads the server, which lacks the queued row — the D10 offline-read gap) → create now navigates only when synced, else confirms "saved offline" + refreshes the list. Residual (D10): offline-created customers aren't visible in reads until sync — the global fix is the offline read-cache/PowerSync behind the reads layer. Potential minor parity gap: the web customer detail may list the customer's jobs; mobile shows contact + sites + notes (jobs list can be added later) | Customers is office/online-primary; the online path is complete + tested (5 repo tests). Offline visibility is the known interim gap, mitigated for the create flow |
| D32 | Invoice + Quote **create/edit builders** built (customer picker, line-items editor w/ add-from-catalogue + live totals, native due/valid-until date pickers) + quote **accept/decline** on the detail. Via the FinanceRepository (outbox + draft). Self-review gaps documented (Q15) | The write repo was already TDD'd; these are the UIs. Edit keeps the customer read-only (matches web). Accept/decline shown only for `sent` quotes (matches web) |
| D33 | Admin areas built: **Staff** (list + role/active + pay-rate editor), **Settings** (integration status), **Reports** (first-pass KPIs). **Staff writes are DIRECT supabase (online-only), NOT via the outbox** — `staff_cost_profiles` has a `staff_id` PK, not the `id` PK the generic outbox upsert assumes; changing the engine would have broken existing test assertions, and Staff is an admin/online task. **Settings** is read-only status (Xero tenant / Google connected); the OAuth connect/disconnect flows stay web-only (browser redirect). **Reports** is a first-pass: revenue (paid/outstanding via MoneyText), active-jobs + quote-win-rate StatCards, and a jobs-by-status bar breakdown — **no Skia/victory charts yet** (deferred to avoid a heavy chart dep; the web keeps the trend/payroll charts). See Q16 | Deliver the admin areas' core value without an engine change (Staff PK) or a heavy chart lib (Reports); money everywhere via MoneyText |
| D34 | Catch-up adversarial review (multi-agent, once the weekly cap reset) over ALL the accumulated office/admin + builder work found **8 real defects — all fixed**: **(high)** invoice edit wiped `work_description` → removed from the edit payload (preserve-on-update); **(high)** Fleet write affordances (add/edit/deactivate) shown to non-admin office users though equipment writes are admin-only → gated behind `useIsAdmin`; **(med)** equipment edit wiped `purchase_date` → omitted from the update payload; **(med)** `removeSite` hard-delete failed FK for referenced sites → delete affordance removed (Q17); **(med)** `saveStaff` swallowed Supabase errors → now throws so the UI surfaces failures; **(med)** Reports quote-win-rate used accepted/total → fixed to accepted/(accepted+declined) matching web; **(med)** date pickers stored a day early via `toISOString` UTC shift → new `localDateKey` (local YYYY-MM-DD) used in all 4 builders; **(low)** Fleet `hourlyRate` at target_hours=0 → returns 0 like web | The preserve-on-update pattern (don't send columns the mobile form doesn't manage) is the key lesson — flagged to apply to any future partial-update screen |
| D71 | **Q20's minor read-visibility gaps closed (last audit residuals, TDD).** The three count/ordering parity gaps the D69 audit left documented are now fixed: (1) **Fleet inactive equipment** — a "Show inactive" toggle (admin) on the fleet list now lists deactivated items with a **Reactivate** action, mirroring the web fleet page + the Pricing inactive pattern (new `listInactiveEquipment` read + `EquipmentRepository.reactivateEquipment` via the outbox + `useFleet` method; +1 test assertion). (2) **Customer-360** jobs/quotes no longer capped at 20 — the section counts now match the web's true totals (screen still slices for display). (3) **Reports** now shows the web's two missing headline metrics — **Overdue** (Σ overdue-invoice totals) and **Accepted quotes** value (Σ accepted-quote totals) — added to `getReportSummary` + two new KPI cards. 200 tests green, tsc + iOS bundle clean. **Q20 is now fully closed** — no audit residual remains open. | Finishes the audit remediation to zero in-repo: every reviewer finding from D69 is now either fixed or is an external gate; these were the last "documented for later" items and they were cheap to close now |
| D70 | **Write-result reporting hardened — Q20's systemic gap now actually fixed (TDD).** The D69 audit's #1 robustness item was that `synced` reports *connectivity*, not server *acceptance*, so a permanently-rejected write while online reported success and a create-then-navigate flow routed to a phantom row. Rather than leave it deferred, added a proper per-op outcome path: new `Outbox.writeStatus(rowId)` → `"settled" \| "pending" \| "failed"` (**failed** = any of the row's write ops is failed/dead — the server rejected it or it's retrying; **failed** deliberately wins over **pending** so a partially-failed compound write surfaces) + a thin `useWriteOutcome()` hook. Every create-then-navigate / optimistic-insert screen now checks it after the flush: **invoices/new, jobs/new, quotes→convert-to-job** show "couldn't save — queued & will retry" and stay put instead of navigating to a row that never persisted; **pricing, inventory** surface the same alert instead of the row silently vanishing on reload. +2 outbox tests (200 total). This closes the systemic half of Q20; only the minor count/ordering read-visibility gaps remain (still non-blocking). | The dead-letter badge already surfaced these failures eventually; this makes the failure *immediate and per-action*, and stops the misleading navigation to a non-existent detail — the concrete symptom the auditor called the single biggest robustness gap |
| D69 | **Cleanup-phase whole-codebase audit + remediation loop (5 independent reviewers).** Ran a granular final gap/drift/defect sweep over the ENTIRE mobile app across 4 dimensions (offline/outbox correctness, money-safety/role-gating, read-layer web-parity, RN correctness/error-handling) PLUS the directive's nominated non-Anthropic validator — **PAL `precommit` with `gemini-2.5-pro` (google)**, which this time completed and independently confirmed the money-safety invariants, offline idempotency, and the D66 remediation. **Money math + technician dollar-blindness verified structurally intact** (MoneyText + rate-stripped `*_public` views + `isOfficeOrAdmin`-gated base-table reads + `Stack.Protected` route registration — reviewer confirmed no raw-`$` tech leak and no fail-open role default). **Fixed this pass:** ⚠️ **CRITICAL offline bug** — the outbox gated *delete* ops on a not-yet-synced same-row insert but **not update** ops, so an offline insert+update pair (e.g. clock-in then clock-out) could silently lose the update if the insert transiently failed on reconnect (`updateRow` on 0 rows is a no-op, marked done); generalised the guard to updates (preserving any author-set `dependsOn` for compound writes) + 3 regression tests. **MED/LOW fixes:** reclaimInflight now charges an attempt so a crash-looping op eventually dead-letters; variation approval honours a deliberate quantity 0; equipment-usage signed-URLs batched into one setState + `total_cost` precomputed in the read; `on_hold` added to the Reports status breakdown (web parity); job-detail shows a proper not-found state instead of an infinite spinner; the customers favourite toggle invalidates in-flight searches + re-sorts on revert; null `usage_date` guarded; catch/alert added to the catch-less write screens (invoices/new, quotes accept-decline, billing add-item); the one raw-`$` chip (invoices/new) wrapped in MoneyText; PO list ordered. 198 tests green, tsc + iOS bundle clean. Remaining reviewer items are documented (Q20) — the systemic `synced`-means-online (not write-accepted) reporting gap, which already has the dead-letter badge as its mitigation, plus a few minor read-parity visibility gaps | The audit's headline catch was a genuine data-loss bug (a silently-lost clock-out = lost billable/payroll time) that unit tests and per-feature reviews had missed because it only manifests on a transient insert-failure during offline replay; the fix is symmetric to an existing, tested delete guard |
| D68 | **Technician variation submit now offline-durable (gap-analysis P2, My Jobs, TDD)** — the tech "Log Variation" flow was the last field-tech write still doing a **direct** supabase insert + direct photo upload (not offline-safe). It now routes through the outbox: new `VariationsRepository.submitVariation` inserts via the `job_variation` aggregate with a client-UUID PK, and the optional photo rides the **attachment queue** (upload-before-row, keyed `<jobId>/variations/<rowId>.jpg` in `job-photos` via `attachmentPathField: "photo_storage_path"` → idempotent replay). It still **never sends rate/total_amount/status** (the `apply_variation_pricing()` trigger prices server-side — a tech can't see or set a figure). `useVariations.submitVariation` persists the picked photo to durable storage first (mirrors expense receipts); the tab shows "Saved offline — will sync" when queued. Removed the now-dead base64 upload path. +2 repo tests. With this, the whole **technician offline-write core** (time / photos / notes / signature / voice / **variations** / backflow register) is uniformly outbox-backed | Closes the last non-durable field-tech write — the app's offline-first thesis now holds for every technician action except the backflow *test-submit* (Q3 dedupe, deliberately deferred) |
| D67 | **Fleet equipment documents — view/open (gap-analysis P3, Fleet)** — the equipment detail now has a **Documents** section listing the item's registration / insurance / compliance / service files (`equipment_documents`, migration 0023) with a tap-to-open via a short-lived signed URL from the private `equipment-documents` bucket. New `listEquipmentDocuments` + `getEquipmentDocumentSignedUrl` reads. **View/open only — upload stays a web action**, matching the established mobile document story (`JobDocumentsTab` is likewise view-only; document *upload* needs a native file/PDF picker deliberately deferred to the web, so no unverifiable native module is added). With this, the only remaining Fleet item is closed — Fleet is at **full parity** for in-repo scope | Closes the equipment-documents gap consistently with how job documents already work on mobile; upload deferral avoids a device-only native dependency that couldn't be QA'd here |
| D66 | **Adversarial review of the D63/D65 money paths → 1 real drift fixed (remediation loop).** An independent reviewer diffed the variation-approval + job-equipment-usage additions against the web line-by-line. **Confirmed byte-for-byte correct:** `priceAndApprove`/`reject` payloads (incl. `total_amount = rate×quantity`), `computeEquipmentCost`, and the **technician dollar-blindness invariant** (office read gated by `useIsOfficeOrAdmin`, techs read the rate-stripped view, every figure via role-gated `MoneyText`, no raw-Text dollar leak). **Fixed (MED):** `getJobEquipment` priced usage on a *since-deactivated* fleet item at full cost, but the web (and the mobile **Costing** screen, `jobCosting.ts`, which filters `is_active`) treat it as **$0** — mobile Billing now prices every usage row from the **active-equipment map** (byte-for-byte the web's `equipmentById`), so Billing ≡ Costing ≡ web. Also matched the web's `.toFixed(1)` hours formatting (cosmetic). **Non-issues:** the billing route is registered only inside `Stack.Protected guard={isOffice}` (a technician never registers it — the reviewer's "no component guard" caveat is covered at the routing layer); mobile rejects a blank approval rate where the web silently approves at $0 (mobile is deliberately stricter/safer) | The review's one substantive catch was a real cross-platform + cross-screen total mismatch on historical usage of deactivated equipment; pricing via the active map fixes it and keeps mobile self-consistent |
| D65 | **Variation pricing + approval (gap-analysis P3, Jobs, money-critical, TDD)** — the last open Jobs item. Office/admin can now **price + approve** or **reject** a technician's custom (pending) variation from the job Variations tab, mirroring the web (`components/job/job-variations.tsx` priceAndApprove/reject). New `VariationsRepository.priceAndApprove` (writes `rate`, `quantity`, **`total_amount = rate × quantity`**, `status="approved"`, `approved_by`, `approved_at`, `admin_notes`) / `reject` (`status="rejected"` + notes + metadata, **no money columns**) — durable via the `job_variation` outbox aggregate; 3 tests lock the payloads incl. the total. New `getJobVariationsForApproval` read hits the **base** `job_variations` table (rate/total_amount — office/admin RLS) while technicians still read the rate-stripped `job_variations_public` view; the tab branches on `useIsOfficeOrAdmin`. Pricing controls + priced totals are office/admin-gated **and** every figure renders through role-gated `MoneyText` (defence-in-depth — a technician build shows nothing). This feeds the approved-variation → invoice reconcile (D44) and job profitability (D37). **Jobs is now full parity** except customer/site reassignment (web) | Closes the money-bearing variation-approval workflow with the proven outbox write; the rate-stripped-view-vs-base-table split keeps the tech dollar-blindness structural, not per-screen |
| D64 | **Job title / type / description edit (gap-analysis P3, Jobs, TDD)** — the office/admin job quick-edit sheet (previously status + priority only, D43) now also edits **title**, **job type** (service/installation/maintenance/emergency/quote/project — the web's full set), and **description**. Status/priority/type chips save instantly; title + description save together via a "Save details" button. All through the existing preserve-on-update `JobsRepository.updateFields` (extended with `title`; only changed fields are written, blank title rejected) on the durable outbox; optimistic local update. +1 repo test. **Customer / site reassignment stays a web action** (it invalidates the site + assignee and re-runs calendar sync — a heavier flow) — noted in the sheet. Remaining Jobs item: price+approve variations (the variation approval workflow) | Closes most of the Jobs edit gap with the proven single write path; customer/site reassignment is deliberately left to the web where the dependent-field cascade is already handled |
| D63 | **Job equipment-usage on the billing screen (gap-analysis P3, Jobs/Fleet, TDD)** — mirrors the web job **Equipment** tab. The billing screen now has an **Equipment usage** section: log which fleet items were used on the job (equipment picker from active equipment + date + hours + notes), priced at each item's computed cost-per-hour (the verbatim-ported, TDD-locked `computeEquipmentCost`), with a hours-logged + total-cost subtotal and long-press to remove. Extended `EquipmentRepository.addEquipmentUsage` with an optional `jobId` (general usage still defaults `job_id` null) — the write stays on the durable outbox; +1 test. New `getJobEquipment(jobId)` read (usage joined to equipment for name+cost, plus active-equipment options). Office/admin-only (billing route is gated; all money via `MoneyText`). This is the hours-used input the admin **Job costing / profitability** screen (D37) already reads via `equipment_usage_log.job_id` | Closes the job equipment-usage gap so a job's real equipment cost rolls into profitability, reusing the proven outbox write + the byte-for-byte cost math; the only remaining job-billing item is PO/cost-centre **editing** (still web-managed — documented in the screen footnote) |
| D62 | **Customers favourites (gap-analysis P3, Customers, TDD)** — mirrors the web's pin-favourites behaviour. `customers.is_favorite` is now surfaced (`CustomerListRow`/`CustomerDetail` + LIST select) and `listCustomers` orders **favourites-first, then name** (`.order("is_favorite", desc).order("name").order("id")`). New `CustomersRepository.setFavorite(id, bool)` writes ONLY the `is_favorite` column through the outbox (preserve-on-update — a favourite toggle can't clobber other fields; +2 tests) → `useCustomers.setFavorite`. UI: a star toggle on each customers-list row (optimistic flip + local re-pin, revert + alert on write failure) and a star in the customer-detail header. With this the **Customers area is at full parity** for in-repo scope (remaining: none in-repo; Send/PDF for related invoices/quotes stay the external Bearer gate) | Closes the last Customers gap with the standard outbox single-column write; ordering matches the web so the pinned set is identical on both platforms |
| D61 | **Fleet: general equipment usage log (gap-analysis P3, TDD)** — the equipment detail now has a **Usage log** section: add (date optional / hours / notes) + remove + total hours. New `equipment_usage` aggregate + `EquipmentRepository.addEquipmentUsage` (general usage → `job_id` null; DB defaults the date) / `removeEquipmentUsage` via the outbox — 2 tests. `listEquipmentUsage` read + `useFleet` methods. **This is the hours-used input behind the Reports Equipment Utilisation table** (D52). Only Fleet item now remaining: equipment **documents** (file uploads — like job documents) | Completes the Fleet expense+usage story and feeds the utilisation report's hours-used with real data |
| D60 | **Quick-create from a customer (gap-analysis P3, Customers)** — the customer detail now has **New Job / Quote / Invoice** buttons that deep-link to the create screens with the customer **pre-selected** (`?customerId=`). All three create screens (`jobs/new`, `quotes/new`, `invoices/new`) accept the param and prefill the customer (jobs/new also loads their sites); invoices/new only prefills the customer when not already prefilling from a job. Remaining Customers item: **favourites** (mark + favourites-first ordering — minor) | Closes the quick-create Customers gap by reusing the existing create screens with a param — no duplicated flows |
| D59 | **Settings: cost-centre templates management (gap-analysis P3, admin, TDD)** — the standard PO cost-centre stages (grouped by `group_name`) can now be managed from Settings: list (grouped) + add (group / stage name / code) + an eye toggle to deactivate/reactivate. `cost_center_template` aggregate + `SettingsRepository.createCostCentreTemplate`/`setCostCentreTemplateActive` via the outbox (2 tests, using a shared `writeTo` helper). `listCostCentreTemplates` read + `useSettings` methods. With this, the only remaining Settings item is **Xero account codes** (web-configured, documented) + integration OAuth connect (external gate) | Reuses the same outbox CRUD pattern as variation types; Settings is now effectively at parity for the in-repo config |
| D58 | **Staff leave log (gap-analysis P3, admin)** — the admin staff editor now has a **Leave log** section: view / add / remove `staff_leave` entries (type sick/annual/public_holiday/other, start+end dates, hours, notes). Direct writes (`listLeave`/`addLeave`/`removeLeave`, admin-only RLS `is_admin`) per D33 — consistent with the staff cost editor. This is the leave data that feeds the Reports **staff efficiency** utilisation/true-cost math (worked vs paid hours). With this the Staff area is at parity except **invite/resend/edit-login-email** (Supabase auth-admin — external gate) | Closes the leave-log Staff gap; the write path matches the existing direct-write staff pattern, and it completes the staff-efficiency data inputs |
| D57 | **Settings: variation-types management (gap-analysis P3, admin, TDD)** — the Settings screen was read-only integration status; admins can now manage **variation types** (the standard-variation catalogue with preset per-unit rates + auto-approve). New `variation_type` aggregate + `SettingsRepository` (create / update name+unit+rate+auto_approve preserve-on-update / soft-activate via the outbox — 3 tests). `listVariationTypes` read + `useSettings` hook. Settings section: list (rate via MoneyText, auto-approve + active state) + add/edit modal (name/unit/rate + auto-approve switch) + an eye toggle to deactivate/reactivate. Remaining Settings: cost-centre templates + Xero account codes + sync-now triggers (web-managed, documented) | Closes the biggest Settings gap with the standard outbox CRUD; deactivation is a soft-delete so existing job_variations keep their type |
| D56 | **Equipment detail screen + expenses/service history (gap-analysis P3, TDD)** — new `app/fleet/[id].tsx` (reached via a › on each fleet row): shows the equipment's **running-cost breakdown** (depreciation/fixed/fuel per year + $/hr via the tested `computeEquipmentCost`), assignee, and its **expenses & service history** — add (category chips service/repair/fuel/tyres/registration/insurance/other, supplier, amount/GST, date, **image receipt**) + remove + view-receipt. `EquipmentRepository.addEquipmentExpense/removeEquipmentExpense` reuse the **exact job-expense attachment pattern** (upload-before-row, `receipt_storage_path` via `attachmentPathField`, idempotent key, `equipment-documents` bucket). 3 tests. New `getEquipment`/`listEquipmentExpenses`/`getEquipmentReceiptSignedUrl` reads + `useFleet` methods (receipt persisted before enqueue). Closes two Fleet gaps (detail screen + expenses); remaining: equipment documents + general usage log | Reuses the proven offline receipt-attachment path; the detail screen was itself a parity gap |
| D55 | **Adversarial review of the session's money-critical additions → 3 parity drifts fixed (remediation loop).** A reviewer diffed every ported pure function line-by-line against its web original. **Verbatim-correct (no drift):** `costing.ts`, `staffEfficiency.ts`, `equipmentUtilization.ts`, `topCustomersBySpend`, `jobCosting`/`reports` query filters, and the compound outbox writes (`approveJob`/`sendBackJob`, `createJob`/`createJobFromQuote` — job_number never sent + dependsOn-gated items, `createInvoice` reconcile). **3 real drifts FIXED to match the web exactly:** (1) `summarizeCustomerInvoices` — the web Customer-360 counts total = ALL invoices incl. cancelled and outstanding = not-paid-and-not-cancelled (incl. draft); mine had excluded cancelled/draft. (2) `revenueByMonth` bucketed by UTC month; now buckets by **Melbourne business-TZ** month (injected extractor) + `lastNMonthKeys` uses business-TZ — so boundary-month invoices land in the same month as the web. (3) `jobsByStaff` now **excludes unassigned** jobs (web has `if (!assigned_to) return`). Tests updated to the web definitions. Re-flagged (already Q19/D39): the dollar-leak RLS migration 0035 is still unapplied — the UI gates aren't a security boundary until Jason applies it | The pure-math + compound-writes were byte-for-byte correct; the drift was in the three aggregations I'd derived from the wrong web source — now matched exactly so mobile ≡ web figures |
| D54 | **Fleet: assign equipment to a staff member (gap-analysis P3, admin)** — `EquipmentRepository.assignEquipment(id, staffId|null)` (preserve-on-update: only `assigned_to`, via the outbox; +1 test). The fleet read now joins the assignee (`equipment_assigned_to_fkey`) and shows it in the list row; admins **long-press** an item → a staff picker (assign/unassign). This is the input that folds a vehicle's $/h into that person's loaded cost rate (costing/staff-efficiency). Remaining Fleet depth: equipment expenses/documents/usage log, edit purchase_date, reactivate — follow-ups | Closes the assign-equipment Fleet gap with the preserve-on-update pattern; it's the missing driver of the loaded-cost vehicle fold-in |
| D53 | **Staff charge-out rate editable (gap-analysis P3, Staff depth)** — the admin staff cost editor now includes the per-employee **charge-out (billing) rate override** (`staff_cost_profiles.charge_out_rate`, migration 0026): surfaced in `listStaff`, saved via `saveStaff` (blank clears the override → default charge-out applies), with a field in the editor. Direct-write (online, admin) per D33. Remaining Staff depth: the **leave log** (view/add/delete `staff_leave`) + roster detail (phone/vehicle in the row) — follow-ups | Closes the charge-out half of the Staff gap; it feeds the web's labour-billing charge-out |
| D52 | **Equipment Cost & Utilisation table built — Reports set now COMPLETE (gap-analysis P3, TDD)** — the last Reports table. Pure `lib/equipmentUtilization.ts` reusing the tested `computeEquipmentCost`: 12-month hours-used per item, utilisation % (used ÷ target), and true cost per hour used (fixed annual cost ÷ hours used + fuel/h) — 3 tests. `getEquipmentUtilization` read (equipment × equipment_usage_log, 12 months). Rendered for **office+admin** (not payroll-sensitive, matching the web). With this, **all 5 Reports analytics tables** (revenue-by-month, top-customers, jobs-by-staff, staff cost/efficiency, equipment utilisation) plus the KPI first-pass are done | Reports is now at full parity; low utilisation surfacing an inflated true $/hr is the "hire vs own" signal the web intends |
| D51 | **Staff Cost & Efficiency table built (gap-analysis P3 'high', admin-only, TDD)** — the last high-value Reports gap. Pure `lib/staffEfficiency.ts` (`computeStaffEfficiency`) ported verbatim from the web Reports page, **reusing the already-tested `computeLoadedCost`/`computeEquipmentCost`**: loaded hourly rate (with assigned-vehicle $/h folded in), 12-month worked/leave/sick hours, utilisation % (worked ÷ paid), and **true cost per worked hour** (fully-loaded annual cost ÷ hours worked). 3 tests lock the figures. `getStaffEfficiency` read (staff_cost_profiles × time_entries × staff_leave × equipment over 12 months). Rendered as an **admin-only** Reports section (`useIsAdmin` gate + RLS backstop; money via MoneyText). Only the **Equipment Cost & Utilization** table now remains from the Reports set | Payroll analytics closed by reusing the verbatim-ported, TDD-locked cost math; admin-gated because it derives from wage data |
| D50 | **Quotes convert-to-job built (gap-analysis P3, TDD)** — an accepted quote can now be converted to a job. `JobsRepository.createJobFromQuote` mirrors the web quote-detail convert: create a pending service job (title/description/customer/site, `job_number` never sent), copy the quote's items to `job_items`, and link `quotes.job_id` — the items + link are **gated on the job insert (dependsOn)** so they never land before the job exists; all durable via the outbox. 2 tests. Quote detail shows a **Convert to Job** button on accepted quotes (and a "Converted — open" link once done). Optimistic; offline → queued | Closes the Quotes workflow gap with the same dependsOn-gated compound-write pattern as the invoice reconcile |
| D49 | **Two list-parity gaps closed (gap-analysis P3, TDD)** — **Pricing reactivate:** `listInactivePricing` read + `FinanceRepository.reactivatePricingItem` (via outbox) + a "Show inactive items" toggle on Pricing that lists deactivated items with a Reactivate action (they were previously invisible on mobile). **Inventory low-stock:** pure `lib/inventoryStock.ts` (`isLowStock` = qty ≤ reorder level, matching the web) + an aggregate "N items at or below reorder level: …" banner on the Inventory list (per-row highlighting already existed). 4 tests total | Quick, clean parity wins closing two Inventory/Pricing gaps; the low-stock rule is TDD-locked to the web's `<=` threshold |
| D48 | **Reports analytics tables built (gap-analysis P3, TDD)** — Reports was a KPI-only first-pass; it now also renders **Revenue by month** (6-month paid-vs-outstanding bars), **Top customers by spend** (top 8), and **Jobs by staff** (completed/total + rate). Pure `lib/reportsAnalytics.ts` (`topCustomersBySpend` / `revenueByMonth` / `jobsByStaff`) with 3 tests; `getReportAnalytics` read (last-6-month keys + name joins). Rendered with lightweight RN bars — **no Skia/victory dep** (avoids an unverifiable native chart lib). All money via MoneyText. Deferred: the **Staff Cost & Efficiency** + **Equipment Cost & Utilization** payroll tables (they need `staff_leave` × time-entries × `computeLoadedCost` and equipment-usage joins — a bigger admin-only follow-up; the ported cost math is ready to reuse) | Closes 3 of the 5 Reports gaps with tested pure aggregations + dependency-free bars; the two payroll-heavy tables are the remaining follow-up |
| D47 | **Customer-360 built (gap-analysis P3 'high', TDD)** — the customer detail showed only contact + sites; it now also shows a **Financials** rollup (total invoiced / outstanding, via MoneyText) and **Jobs / Quotes / Invoices** sections (recent, counted, tapping deep-links to each). New `getCustomerOverview` read + a pure `summarizeCustomerInvoices` (total invoiced = non-cancelled; outstanding = sent + overdue) with 2 tests. Customers is office/admin-only so the dollar rollups are role-safe. Deferred: favourites, active/inactive lifecycle, quick-create-from-customer (smaller follow-ups) | Closes the highest-severity Customers gap (the 360 view) with a tested rollup; reuses the money-safe MoneyText |
| D46 | **Backflow register-device now offline-durable (gap-analysis P2, TDD)** — the tech backflow writes were all direct supabase inserts (not offline-safe). `BackflowRepository.registerDevice` now routes registration through the outbox (new `backflow_device` aggregate, client-UUID PK, `is_active:true` so it shows immediately, replays idempotently). `backflow/new.tsx` uses it: synced → device detail, offline → "queued" + back. 2 tests. **NOT yet offline:** logging a TEST — it carries a certificate/signature upload AND a water-authority **submit** that must be dedupe-guarded so a retry can't double-email the authority (Q3); that compound flow (attachment-before-row + a `backflow-submit` side-effect with server-side dedupe) is a careful follow-up, deliberately left direct-write for now so the compliance path isn't destabilised | Registration is the safe, high-value offline win; the compliance test-submit needs the dedupe guarantee (Q3) before it can be made replayable |
| D45 | **Create-job flow built (gap-analysis P1, TDD)** — mobile had no way to create a job; office/admin now can (a "New" button on the office Jobs list). `JobsRepository.createJob` inserts via the outbox with a client-UUID PK for idempotent replay; **`job_number` is a DB serial so it's never sent**; only customer_id + title are required (the rest carry DB defaults). A coalesced `sync-calendar` follows the insert when a schedule is set. 3 new tests. Screen reuses the CustomerPicker, the job's sites (via `getCustomer`), the `listAssignableStaff` picker, type/priority chips, and a date picker (time defaults to 8am local — full start/end time editing stays a web action). All four gap-analysis **P1 office workflows are now closed** (Approvals, Schedule, Invoice-from-job, Jobs create/edit) | The last core office gap; the DB-serial job_number + client-UUID PK make offline job creation safe to replay |
| D44 | **Invoice-from-job reconciliation built (Q15 RESOLVED, gap-analysis P1, TDD)** — the mobile invoice builder was generic; it now has a **create-from-job** flow reachable by tapping a **Ready-to-Invoice** queue row. Extended `FinanceRepository.createInvoice` with a `reconcile` option: after the invoice + items land, it **marks the pulled-in approved variations billed** (`job_variations.invoice_id = <new invoice>`) and **clears `jobs.ready_to_invoice`** — both gated on the invoice op (dependsOn) so a partial failure never marks a variation billed against a missing invoice. 3 new finance tests. New `getInvoiceJobPrefill` read (customer, title, work description from completion notes/voice transcript, job_items → line items, unbilled approved variations to add as tagged line items). Invoices/new accepts a `jobId` param, prefills, offers variation chips, and passes the reconcile. **Note:** unlike the web it does NOT first call `/api/jobs/[id]/sync-billing` (cookie-only Bearer route — external gate), so job_items may be marginally stale | Closes the reconciliation gap the generic builder left (Q15): a mobile invoice created from a ready job now clears the queue + can't double-bill a variation, matching the web |
| D43 | **Job status/priority quick-edit (gap-analysis P1, TDD)** — office/admin can now change a job's **status** and **priority** from the job detail (tap the status badge → chips). Durable via a new `JobsRepository.updateFields` (preserve-on-update — only the changed field is written, so a status change never wipes the description; a no-op when nothing changed) + `useJobEdit` hook; optimistic local update. Status/priority values validated against the DB CHECK (`pending/scheduled/in_progress/completed/cancelled/on_hold`, `low/normal/high/urgent`). 5 tests. **Scoped to office/admin** — a technician's status is still driven by clock-in (in_progress) and the sign-off flow (completed), so a raw status picker isn't exposed to them (would bypass the signature invariant). Remaining Jobs gaps: create-job, edit customer/site/title/type, price+approve variations, delete (follow-ups) | Closes the office Jobs status/priority edit gap with a durable, preserve-on-update write; keeps the technician completion invariant intact |
| D42 | **PAL external validation used at this gate (non-Anthropic, per the build directive).** Ran PAL `codereview` over the Approvals + Schedule work with **`gemini-2.5-pro` (provider google)** as the tester/validator — the achievable form of the directive's "second agent = tester/validator, non-Anthropic" (Claude is the orchestrator since it's the only agent that can edit the repo; **Fable 5 is not a PAL provider** and the `consensus` tool has repeatedly timed out — see D29). Verdict: **no critical/high logic defects**; it praised the outbox `dependsOn` chaining + the DST-safe date port. Applied its two clear wins: optimistic Schedule updates (avoids stale-after-reassign offline) + a stable staff sort. The Xero/PDF/send external gates remain | Genuinely exercises the nominated non-Anthropic validation path; keeps the money-adjacent approve/reschedule logic externally checked, not just self-reviewed |
| D41 | **Schedule dispatch built (gap-analysis P1, TDD)** — the Schedule board was read-only; office/admin can now **reassign** a job to a technician (or unassign) and **reschedule** it to another day. Tap a scheduled job → action sheet (Reassign / Reschedule / Open). Both are durable `job` updates via a new `ScheduleRepository` (reassign and reschedule are separate preserve-on-update writes, mirroring the web's two drag paths) each followed by a coalesced `sync-calendar` side-effect. Reschedule keeps the wall-clock time-of-day and the original duration via a **ported, DST-safe** `computeReschedule` / `withDateKeyPreservingTime` (Melbourne-explicit `Intl` offset lookup, ported verbatim from the web `lib/date.ts`) — 6 date tests + 4 repo tests lock it. Technician picker uses a payroll-free `listAssignableStaff` read (office-safe). Day/week grid + drag-drop is deferred (touch list is the mobile-native equivalent) | Closes the other read-only office workflow; the DST-safe reschedule math is money-adjacent-sensitive (wrong day = wrong dispatch) so it's ported verbatim + fully tested |
| D40 | **Approvals actions built (gap-analysis P1 remediation, TDD)** — the mobile Approvals screen was read-only; now admins can **Approve** a completed job and **Send Back**. Approve mirrors the web exactly: dup-guard on an existing invoice, else auto-create a **draft** invoice (+ items) from the job's line items (reusing `FinanceRepository.createInvoice` through the outbox), then mark the job `admin_status=approved` with `ready_to_invoice = !invoiceId`. Send-Back requires a note → `admin_status=rejected`, `status=in_progress`, `ready_to_invoice=false` + a coalesced `sync-calendar` side-effect. `ApprovalsRepository` (4 tests) + `getApprovalPlan` read + `useApprovals` hook + expandable admin cards (notes field, overtime badge) with an office view-only banner. **The web's Xero auto-push on approve is an EXTERNAL GATE** (cookie-only push-invoice route) — the invoice is left as a draft to push from the web | Highest-impact office gap from the gap analysis; reuses the proven outbox invoice path so the multi-step write is durable + idempotent |
| D39 | **MP1 dollar-leak RLS — drafted (not applied) via a multi-agent workflow.** Audited 10 candidate money tables (current policy + money columns + whether any technician-reachable mobile route touches them), synthesised a proposed tightening migration + role-impersonation test, and adversarially verified it through 3 lenses (remaining-leak / broken-tech-access / SQL-correctness — all **approve**). Then I manually confirmed the safety-critical claim (grep: no technician mobile route reads equipment/inventory) and verified every `drop policy` name matches the real policy in the migrations (so no permissive policy survives to OR-leak). Deliverables: `supabase/migrations/0035_...sql` (locks job_expenses, equipment_expenses, inventory, equipment-SELECT to office/admin, mirroring 0027) + `supabase/tests/0035_rls_role_impersonation_test.sql`. **Explicitly NOT applied** — RLS is untestable from the mobile repo and `supabase/` is Jason's; both files are flagged PROPOSED for his review + live test + apply. Two column-level cases (purchase_orders/po_cost_centers) deferred with a documented view/column-privilege plan (Q19) | The workflow's value was concrete: it caught that purchase_orders/po_cost_centers can't be blanket-locked (techs read their non-money columns) — my first instinct was to lock them, which would have broken the tech Time/Overview tabs |
| D38 | **Adversarial review of the whole session diff (`ee4f79b..HEAD`, mobile/) — CLEAN, no high-confidence defects.** An independent reviewer read the diff + the web originals + the Supabase migrations and confirmed: `lib/costing.ts` is a faithful line-for-line port of the web money math (formulas, filters, rounding all match); `jobCosting.ts` queries match the web page; the costing screen is admin-only (route `Stack.Protected isAdmin` + `useIsAdmin` link + RLS defense-in-depth); the `attachmentPathField` generalization defaults to `storage_path` and no existing repo sets it (photo/voice/signature behaviour unchanged); expense insert/delete payloads + idempotent receipt key are correct; no expo-router route collision; all column names match migrations (incl. `time_entries.hours`, `billing_rate_config.id boolean`). **The review also surfaced a pre-existing dollar-leak (see the elevated Q below).** | Session gate passed with external-style validation; the one real risk it found is a backend RLS gap, not a mobile-code defect |
| D37 | **Admin job costing / profitability (Q18)** built (TDD): ported the web's payroll cost math **verbatim** into `mobile/lib/costing.ts` — `computeLoadedCost` (fully-loaded staff rate: wage × on-cost multiplier + fixed oncosts + assigned-vehicle $/h), `computeEquipmentCost` (straight-line depreciation + fixed + fuel per hour), and a new pure `computeJobProfitability` rollup (labour + materials + **unassigned** equipment vs invoiced revenue, projected margin from built-up items + unbilled approved variations, below-min-margin flag, uncosted-hours warning). **11 classical tests lock every figure to hand-computed values** incl. the assigned-vehicle double-count exclusion and divide-by-zero guards. Read `lib/data/reads/jobCosting.ts` mirrors the web page's queries (min margin from `billing_rate_config`, default 30). Screen `app/job/[id]/costing.tsx` is **admin-only** (`Stack.Protected` isAdmin — payroll-sensitive), reached from a "Job costing & margin" link on the billing screen (admin-only), all money via MoneyText | Job profitability is core office value and the math is money-critical, so it's ported byte-for-byte and TDD-locked; admin-gated because it derives from payroll data technicians must never see |
| D36 | **Office/admin backflow access (Q12)** built (TDD): extracted the list logic into a tested read (`lib/data/reads/backflow.ts` → pure `computeBackflowRows`: latest-passing-test selection, fail-filtering, worst-first sort) and a tested presentational `BackflowDeviceRow`. The technician tab was refactored onto both (no divergence). Added `/backflow/list` (office group, native header) linked from the **More hub** + the now-tappable **Dashboard "Backflow Due" KPI**. Distinct route from the tech tab's `/backflow` to avoid an expo-router route collision (a group segment is invisible in the URL) | Close a real nav gap without duplicating the due-date/sort logic; the shared read/row are the single source of truth for both roles |
| D35 | **Expense capture with receipt upload** built on the Job Billing screen (TDD): `addExpense`/`removeExpense` route through the outbox (`job_expense` aggregate → `job_expenses`), with the **receipt attached via the same upload-before-row attachment path as Photos**. To reuse the processor cleanly I **generalized it with an optional `attachmentPathField`** (defaults to `storage_path`; expenses set `receipt_storage_path`) — fully backward-compatible, all existing photo tests still pass. Receipt object key is derived from the client rowId (`${jobId}/expense-${rowId}.jpg`) → idempotent replay. Receipts share the web's private **`job-documents`** bucket. **Scope:** mobile captures an **image receipt** (camera/gallery); **PDF** receipts stay a web action (documented in the screen footnote). `entered_by` = the signed-in profile id | Closes the biggest Q18 gap using the proven attachment queue; the processor generalization is the only engine change and it's additive + tested |

## Consensus / external validation (PAL)

| # | Decision | Why |
|---|---|---|
| D29 | Re-ran external validation for the "finish to ship" plan. PAL consensus (Fable 5 + GPT-5.6) could not complete — **Fable 5 isn't a PAL provider** and **gemini-3-pro-preview 404s on OpenRouter**; the multi-model consensus timed out. Fell back to a single adversarial review by **`google/gemini-2.5-pro`** (score 100), the sub the original plan already used. Its findings are folded in below | Don't block the build on flaky external tooling; get real non-Anthropic validation via the reliable path. Worker/validation pool stays non-Anthropic/non-OpenAI per Avi (glm-5.2, gemini-2.5-pro, grok, deepseek, qwen, llama) |

### Avi's decisions on the flagged forks (2026-07-22)
- **Build order → "Read-repository layer now":** every screen's reads route through a thin `lib/data/reads/` abstraction as features are built, so a future offline cache (PowerSync or persisted) swaps in cheaply — no per-screen refactor. Applied going forward + retrofitted to the existing office/financial read screens.
- **Financial writes → "Outbox + draft, flag RPC":** multi-step invoice/quote writes route through the durable outbox (ordered + retried), invoices start in `draft` until all line items land (safer than the web's client-side delete-reinsert, which has no retry). **FLAGGED TO JASON:** add an atomic Postgres RPC for invoice/quote writes as the true-transactional hardening.
- **Ship-gate reality (acknowledged):** "100% ship-ready in one go" is not achievable in-repo; GATE 1 (backend Bearer for send/PDF/Xero), GATE 2 (on-device human QA), GATE 3 (cross-system e2e), + store accounts (EAS) + PowerSync password (offline reads) are human/backend gates. Continuous build proceeds on all in-repo work; these gates are flagged, not claimed done.

### Gemini-2.5-pro adversarial findings (CRUCIAL — flagged to Avi)
1. **Build order — reorder recommended:** build the **offline-READ cache right after financial writes**, then build CRUD (Customers/Inventory/Fleet) *against the cache*. Building them online-first forces a costly refactor. (Counterpoint I raised: the offline-critical path is the field tech, already covered for writes; office users are usually online; the intended read-cache is PowerSync, which is BLOCKED on a DB-password step. So this is a real fork with cost either way.)
2. **Financial-write correctness (top 3 risks):** (a) `edit = delete-all-then-reinsert` is **non-transactional** — a partial failure corrupts an invoice (total but no items); needs a Postgres RPC transaction (the web app has this same client-side risk). (b) JS vs Postgres float math → UI total can mismatch the DB `GENERATED` line-total; treat DB as source of truth on refresh. (c) No optimistic-concurrency → two editors silently clobber; add a `version`/`updated_at` guard.
3. **Mobile porting:** don't clone the web "mega-form" — use a guided multi-step **wizard** (Select customer → Line items → Review).
4. **Honest "done" (independently confirms my caveat):** the app is **NOT shippable in-repo alone** — hard gates: **GATE 1** invoice/quote send/Xero (backend Bearer blocker), **GATE 2** on-device human QA (no simulator), **GATE 3** cross-system e2e (create on mobile → verify on web). Plus store accounts (EAS submit) + PowerSync password (offline reads).

## Open questions (resolve in cleanup MP11)

- **Q20 (write-result reporting + read-visibility — FULLY RESOLVED, D70 + D71)**: the whole-app audit (D69) flagged that `useFlush()` returns *connectivity* (`synced`), not per-op server acceptance. **RESOLVED (D70):** `Outbox.writeStatus(rowId)` + `useWriteOutcome()` report per-row acceptance, and every create-navigate / optimistic-insert screen surfaces an immediate "couldn't save — queued & will retry" instead of navigating to / silently dropping a non-persisted row. The audit's three minor count/ordering read gaps are **also RESOLVED (D71)**: Fleet inactive-equipment list + Reactivate, uncapped Customer-360 counts, and the Reports Overdue + Accepted-quote-value metrics. Nothing from the D69 audit remains open.

- **Q19 (CRUCIAL — dollar-leak RLS, backend/Jason)**: Migration 0027 tightened pricing/quotes/invoices/quote_items/invoice_items/job_items to office/admin, but several **money-bearing tables were left on the blanket authenticated policy**, so a technician (who holds the anon key) can read them directly via REST/PowerSync — bypassing the app's MoneyText gating. A **multi-agent audit (10 tables) + adversarial verify (3 lenses, all approve)** — cross-checked by a manual grep confirming no technician-reachable mobile route reads them — produced **proposed migration `supabase/migrations/0035_restrict_remaining_financial_tables_to_office_admin.sql` + role-impersonation test `supabase/tests/0035_rls_role_impersonation_test.sql` (see D39). NOT applied — RLS can't be tested from the mobile repo; Jason to review + run the test + apply.** Findings — **lock-for-all** (techs need zero access, mirrors 0027): `job_expenses`, `equipment_expenses`, `inventory` (cost/sell rates), `equipment` (view only; writes already admin-only per 0016). **Deferred column-level cases**: `purchase_orders` (hide total_value) and `po_cost_centers` (hide allocated_amount) — techs legitimately read their NON-money columns (total_hours on the Overview scoreboard, po_number + cost-centre name/code on the Time tab), so a blanket lock would break the tech app; these need a rate-stripped **view / column privileges** + repointing the two mobile reads (a separate design task, still a live residual leak until done). `cost_center_templates`/`equipment_documents` carry no money; `billing_rate_config`/`staff_cost_profiles` were already admin-only. This is the MP1 security BLOCKER (task #9).

- Q1: `sync-calendar` API route currently posts with no Authorization header — confirm intended auth before mobile replays it offline.
- Q2: Confirm every mobile write target accepts a client-supplied UUID PK (no server-gen-only triggers) for idempotent offline replay.
- Q3: Backflow submit server-side dedupe (must not double-email the water authority on retry).
- Q4: Auth refresh-token lifetime across a long offline workday — confirm acceptable.
- Q5: Expo SDK: mobile/AGENTS.md says v57 but package.json pins SDK 54 — confirm target SDK.
- Q6: Web app does not resync billing when a time entry is deleted (or a cost-center is reassigned). Deleting a billed entry may leave a stale labour line item. Confirm intended, or fix in web + mobile together (see D11).
- Q7: Offline cold-start empty time log (D10) — acceptable interim, or prioritise the persisted read-cache / PowerSync buckets so previously-viewed jobs read offline too?
- Q8: Add a `storage.objects` DELETE policy for the `job-photos` bucket so deleted photos' files are actually removed (fixes a pre-existing orphaned-object leak in web too)? Touches shared `supabase/` → needs Jason coordination (see D12).
- Q9: Build the Signature + Voice-report (job-completion) vertical next? It's the last offline-critical tech-core write path — compound job_photos + jobs update + sync-calendar/transcribe side-effects (see D15).
- Q11 **(verify)**: Does a mobile-signed-off *completed* job actually appear in Approvals? Web Approvals needs `admin_status='pending'`, but the sign-off flow (signature.ts) only sets `status/actual_end/ready_to_invoice` — NOT `admin_status`. `admin_status` isn't in the tracked migrations (live-DB column). Confirm its default so the sign-off → approvals linkage works; if not, the mobile sign-off (and/or web completion) may need to set `admin_status='pending'`.
- Q12 **(RESOLVED, D36)**: Office/admin can now reach Backflow — a shared list screen at `/backflow/list` (registered in the office group), linked from the **More hub** ("Backflow") and the **Dashboard "Backflow Due" KPI** (now tappable). Distinct route from the tech tab's `/backflow` (avoids a route collision), but both render the SAME extracted read (`lib/data/reads/backflow.ts` → `computeBackflowRows`, tested) and the SAME `BackflowDeviceRow` component (tested) so they never diverge. The tech tab was refactored onto the shared read/row too.
- Q18 **(follow-up, job billing)**: The office Job Billing screen now supports line items (add/remove via outbox) AND **expense capture** (add/remove via outbox, with an **image** receipt upload + view-signed-URL) — see D35. Admin **job costing / profitability** is also built (D37). STILL NOT on mobile: **PDF** receipts (web only), **PO create/edit**, and **cost-centre assignment on an expense** (the `cost_center_id` column exists; mobile leaves it Unassigned). These remaining pieces are follow-ups; the web keeps them.
- Q17 **(schema, Jason)**: `sites` has no `is_active` column and `jobs.site_id`/`quotes.site_id` FK it with RESTRICT, so a site with any job/quote can't be hard-deleted. Mobile therefore offers **edit-only** for sites (the delete affordance was removed to avoid a stuck "dead" sync op). Add a `sites.is_active` column for soft-delete if site removal is needed on mobile.
- Q16 **(follow-up)**: Reports is a first-pass (KPI cards + a jobs-by-status bar) — no Skia/victory charts yet (revenue-trend, payroll-cost). Add the chart library + those visualisations if full Reports parity is required on mobile. Also confirm Reports revenue definitions match the web (paid = status 'paid'; outstanding = 'sent'+'overdue').
- Q15 **(RESOLVED, D44)**: The mobile invoice builder now has a **create-from-job** flow (tap a Ready-to-Invoice row) that pre-fills from `job_items` + work description + unbilled variations AND reconciles — marks the included `job_variations.invoice_id` and clears `jobs.ready_to_invoice`, all durable via the outbox (gated on the invoice op). One residual: it does not call `sync-billing` first (cookie-only Bearer route — external gate), so `job_items` can be marginally stale; and per D30 an atomic Postgres RPC is still the ideal transactional hardening (flagged to Jason).
- Q14 **(verify)**: The Fleet screen shows an **estimated hourly rate** computed as `(depreciation[purchase_cost/life] + insurance + maintenance + registration + other)/target_hours + fuel_per_hour` (labelled "est."). Confirm this matches the web's exact equipment cost-model formula; adjust `hourlyRate` in `lib/data/reads/fleet.ts` if the web differs. The stored cost fields themselves are exact.
- Q13 **(gap)**: An admin who also does field work can't reach My Jobs / the technician field IA on mobile (admin lands on the office group). Web exposes My Jobs to admin. Add a "My Jobs" entry to the office IA for admins, or a role-view toggle.
- Q10 **(RESOLVED)**: The sync-status pill is now a global floating overlay (`SyncStatusPill` in `app/_layout.tsx`): invisible when synced, "Syncing N…" while in flight, and a tappable "N not synced · Retry" when writes dead-letter (calls `outbox.retryDead()` + flush). Dead-lettered writes are no longer invisible. A finer per-job affordance could still be added later if desired (would need per-job outbox filtering), but the global indicator closes the ship-blocking gap.
