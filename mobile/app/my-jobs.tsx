// Admin-facing "My Jobs" (Q13): an admin who also does field work can see and
// action the jobs assigned to them. Registered in the office/admin group and
// linked from the More hub. Renders the SAME shared component as the technician
// tab so the two never diverge.
export { MyJobsScreen as default } from "../components/jobs/my-jobs-screen";
